apiVersion: v1
kind: ConfigMap
metadata:
  name: northstar-auth-service-db-configmap
data:
  db-host: "northstar-auth-service-db-svc"
  db-name: "northstar_auth_db"
  db-port: "5432"

---
apiVersion: v1
kind: Secret
metadata:
  name: northstar-auth-service-db-secret
data:
  db-user: "cG9zdGdyZXM="
  db-pass: "cm9vdA=="
type: Opaque

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: northstar-auth-service-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: northstar-auth-service-db-deployment
spec:
  selector:
    matchLabels:
      app: northstar-auth-service-db-deployment
  replicas: 1
  template:
    metadata:
      labels:
        app: northstar-auth-service-db-deployment
    spec:
      containers:
        - name: northstar-auth-service-db
          image: postgres:alpine
          imagePullPolicy: IfNotPresent
          resources:
            limits:
              cpu: "500m"
              memory: "512Mi"
            requests:
              cpu: "250m"
              memory: "256Mi"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: northstar-auth-service-db-secret
                  key: db-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: northstar-auth-service-db-secret
                  key: db-pass
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: northstar-auth-service-db-configmap
                  key: db-name
          volumeMounts:
            - name: northstar-auth-service-db-data
              mountPath: "/var/lib/postgresql/data"
      volumes:
        - name: northstar-auth-service-db-data
          persistentVolumeClaim:
            claimName: northstar-auth-service-db-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: northstar-auth-service-db-svc
spec:
  selector:
    app: northstar-auth-service-db-deployment
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
